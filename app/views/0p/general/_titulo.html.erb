<!-- TITULO -->
<!-- size: determina el tamaño del font -->
<!-- titulo: Es el texto del título -->
<h<%= size%>>
	<% case a %>
	<% when 'index' %>
		<%= "// #{corrige(c)}" %>
	<% when 'seleccion' %>
		<%= "Selecciona #{corrige(c.singularize)}" %>
	<% end %>
	<%= titulo %>
</h<%= size%>>
