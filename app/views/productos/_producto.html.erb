<tr>
	<td><%= render partial: '0p/tabla/show_field', locals: {a: a, campo: producto.producto, objeto: producto} %></td>
    <td><%= producto.disponibilidad %></td>
    <td><%= producto.demora_minutos %></td>

	<td><%= render partial: '0p/tabla/btns', locals: {a: a, objeto: producto} %></td>
</tr>
